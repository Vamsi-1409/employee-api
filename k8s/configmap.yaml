apiVersion: v1
kind: ConfigMap
metadata:
  name: webapp-config
  namespace: go-web-app
  labels:
    app: go-web-app
data:
  PORT: "8080"
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "employee_db"
